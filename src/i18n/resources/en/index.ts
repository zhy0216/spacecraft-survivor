import { common } from './common';
import { ui } from './ui';
import { content } from './content';
import { story } from './story';

/** 整个英文语言包。default export 供 loader map 静态加载。 */
export default { common, ui, content, story };
